strih_v_pdf_formatu__digital_product:
  card_id: "<?= $cards["strih_v_pdf_formatu"]->getId() ?>"
  tag_id: "<?= Tag::GetInstanceByCode("digital_product")->getId() ?>"

fridge__oversized_product:
  card_id: "<?= $cards["fridge"]->getId() ?>"
  tag_id: "<?= $tags["oversized_product"]->getId() ?>"

stove__oversized_product:
  card_id: "<?= $cards["stove"]->getId() ?>"
  tag_id: "<?= $tags["oversized_product"]->getId() ?>"
