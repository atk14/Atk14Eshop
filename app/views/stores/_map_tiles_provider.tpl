{* 
  Map tile provider info
  Should be placed on every page with map,
  otherwise provider would default to OSM maps
*}
{javascript_tag}
  window.mapTilesProvider = "{$map_tiles_provider}";
  window.mapTilesAPIkey = "{$map_tiles_api_key}";
{/javascript_tag}
